# empty_block, empty_phi, unreferenced_blocks_in_phi, copy_propagation, dead_store_elimination
fn divide(n: int, d: int) -> int {
    var r = n;
    var q = 0;

    loop {
        if r < d {
            return q;
        }

        r = r - d;
        q = q + 1;
    }
}

int_prop = divide(100, int_prop);