# remove_unreferenced_functions

property int_prop: int;

fn foo(x: int) -> int {
    return x;
}

# Bar here is unreferenced, so should be removed by the optimisation
fn bar(y: int) -> int {
    return y;
}

int_prop = foo(5);
