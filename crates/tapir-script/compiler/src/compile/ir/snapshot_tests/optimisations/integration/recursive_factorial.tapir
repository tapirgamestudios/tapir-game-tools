# all

property int_prop: int;

fn factorial(x: int) -> int {
    if x == 0 {
        return 1;
    }

    if x == 1 {
        return 1;
    }

    return x * factorial(x - 1);
}

int_prop = factorial(10);
