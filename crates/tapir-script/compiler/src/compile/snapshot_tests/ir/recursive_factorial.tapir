fn factorial(x: int) -> int {
    if x == 0 {
        return 1;
    }

    # logical or not implemented at time of writing this test
    if x == 1 {
        return 1;
    }

    wait;
    return x * factorial(x - 1);
}

var x = factorial(10);
wait;