event fn loopy(number_of_loops: int) {
    var counter = 0;
    loop {
        if counter < number_of_loops {
            int_prop = counter;
        } else {
            break;
        }

        counter = counter + 1;

        wait;
    }
}
